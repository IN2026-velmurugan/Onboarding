[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Techfellowtools"
version = "1.0.0"
description = "Utility tools for learning and experimentation"
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name = "Velmurugan K L" }
]

dependencies = []